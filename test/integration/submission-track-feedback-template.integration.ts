import { createDataFactory } from "../data-factory/index";
import { request, graphql } from "../request";

const datafactory = createDataFactory();

describe(`submissionTrackFeedbackTemplate`, () => {
  // createFeedbackTemplate

  // updateFeedbackTemplate

  // updateFeedbackTemplateAsDefault
  it("create submission track feedback template", () => {
    /*test*/
  });
});
