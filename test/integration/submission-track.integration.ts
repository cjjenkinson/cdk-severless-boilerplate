import { createDataFactory } from "../data-factory/index";
import { request, graphql } from "../request";

const datafactory = createDataFactory();

describe(`submissionTrack`, () => {
  // todo - how to create a user with a business workspace and emulate it recieving new submissions

  // e.g createUser({ workspace: { plan: "BUSINESS" }})

  // submissionTrack

  // shortlistSubmissionTrack

  // moveSubmissionTrack

  // declineSubmissionTrack
  it("submission track id", () => {
    /*test*/
  });
});
